syntax = "proto3";
package protocol;

message packet {
    uint32 cmd_id = 1;
    uint32 user_data = 2;
    uint32 timestamp = 3;
    string body = 4;
}

message session_packet {
    packet common = 1;
    repeated uint64 sessions = 2;
}

message send_packet_rsp {
    int32 result = 1;
}